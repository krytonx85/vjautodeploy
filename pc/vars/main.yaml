#fill below vars for your env

pc_ip: "10.134.87.69"
pc_def_gw: "10.134.84.1"
pc_subnet_mask: "255.255.252.0"
pc_network_name: Rx-Automation-Network


#this is the var that will be converted to json and sent to pe for pc deploy
# do not change below
# defaults to small size


pc_deploy_def_template:
 version: "filled_in_tasks"
 should_auto_register: true
 pc_vm_list: 
 - pc_vm_name: "{{ [ 'PC' , 100 | random() ] | join( '-' ) }}"
 num_sockets: 4
 data_disk_size_bytes: 536870912000
 memory_size_bytes: 17179869184 
 dns_server_ip_list: ansible_dns.nameservers
 nic_list:
 - ip_list:
   - pc_ip
 network_configuration:
  network_uuid: "filled_in_tasks"
  subnet_mask: pc_subnet_mast
  default_gateway: pc_def_gw
